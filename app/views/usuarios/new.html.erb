<% content_for :title, t('.title') %>

<main class="content-container">
  <div class="page-header">
    <h1><%= t('.title') %></h1>
  </div>

  <%= render "shared/form_errors", object: @usuario %>

  <div class="form-card">
    <%= render "form", usuario: @usuario %>
  </div>
</main>