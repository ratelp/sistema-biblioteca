<% content_for :title, t('.title') %>

<main class="content-container">
  <div class="page-header">
    <h1><%= t('.title') %></h1>
    <%= link_to t('.back'), categorias_path, class: "btn btn-secondary" %>
  </div>

  <div class="form-card">
    <%= render "form", categoria: @categoria %>
  </div>
</main>
